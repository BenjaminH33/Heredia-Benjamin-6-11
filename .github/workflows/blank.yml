name: Build and test of Java Project

on : [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    
    - name: echo a String
      run: echo "Hello World"
    - name: echo a CD
      run: |
            mkdir pepe
            cd pepe
            ls >> gg.txt
            
    - name: echo a tail
      run: |
            mkdir javier
            cd javier
            ls >> hola.txt
            tail hola.txt
            
            
            
            
            
